# Config

Create a **auth.ini** in the */src* folder.
```
[FACEBOOK]
username=email
password=secretpassword
```

# Install

```
pip install pipenv
pipenv install
```

# Run
```
cd src
python manage.py run
```